summary: Create a new script
tags:
  - scripts
operationId: scriptsCreate
requestBody:
  description: Body request that contains the ScriptPostDto object
  required: true
  content:
    application/json:
      schema:
        $ref: "../../schemas/scripts/ScriptPostDto.yaml"
responses:
  '200':
    description: Expected response to a valid request
    content:
      application/json:
        schema:
          $ref: "../../schemas/scripts/PagedScriptDto.yaml"
  '403':
    $ref: "../../responses/scripts/ResponseScriptForbidden.yaml"
