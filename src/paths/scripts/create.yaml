summary: Post a new script
tags:
  - scripts
operationId: scriptsCreate
requestBody:
  $ref: "../../requestBodies/scripts/createBody.yaml"
responses:
  '200':
    description: Expected response to a valid request
    content:
      application/json:
        schema:
          $ref: "../../schemas/scripts/PagedScriptDto.yaml"
