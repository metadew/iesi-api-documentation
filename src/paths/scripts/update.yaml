summary: Update one or multiple scripts
tags:
  - scripts
operationId: scriptsUpdate
requestBody:
  description: Body request that contains a list of ScriptPostDto object
  required: true
  content:
    application/json:
      schema:
        type: array
        items:
          $ref: "../../schemas/scripts/ScriptPostDto.yaml"
responses:
  '200':
    description: Expected response to a valid request
    content:
      application/json:
        schema:
          $ref: "../../schemas/scripts/ScriptDto.yaml"