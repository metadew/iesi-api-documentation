type: object
properties:
  uuid:
    type: string
    format: uuid
  type:
    type: string
    enum:
      - database
  labels:
    type: array
    items:
      $ref: '../../schemas/datasets/DatasetImplementationLabelDto.yaml'